<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="product">

	<!-- 나의 pt회원리스트 -->
	<select id="selectlist" parameterType="map" resultType="ProductVo">
		<![CDATA[
			select *
			from product
			
		]]>
		<if test="keyword != null">
			<![CDATA[
				where (p_name like concat('%',#{keyword},'%') or p_category like concat('%',#{keyword},'%'))
			]]>
		</if>	
		<![CDATA[				 
			limit #{startRowNo}, #{listCnt}
		]]>
	</select>
	
	<!-- 전체 글갯수 -->
	<select id="selectTotalCnt" parameterType="string" resultType="int">
		<![CDATA[
			select count(*) count
			from product
		]]>
		<if test="keyword != null">
			<![CDATA[
				where (p_name like concat('%',#{keyword},'%') or p_category like concat('%',#{keyword},'%'))
			]]>
		</if>	
	</select>
	
	<!-- 등록 -->
	<insert id="insert" parameterType="ProductVo">
		<![CDATA[
			insert into product
			values(null, #{p_name}, #{p_price}, #{p_category}, #{p_explanation}, null, null, null, null, null)
		]]>
	</insert>
	<!-- values(null, #{p_name}, #{p_price}, #{p_category}, #{p_explanation}, #{filePath}, #{orgName}, #{saveName}, #{fileSize}, #{p_remarks})-->

	<!-- 삭제 -->
	<delete id="delete" parameterType="ProductVo">
      <![CDATA[ 
      	delete from product
		where p_no=#{p_no}
      ]]>
	</delete>

	<!-- 전체 리스트 -->
	<select id="selectAll" resultType="ProductVo">
      <![CDATA[ 
        select  p_no,
				p_name,
		        p_price,
		        p_category,
		        p_explanation,
		        filePath,
		        orgName,
		        saveName,
		        fileSize,
		        p_remarks
		from product
      ]]>
	</select>
	
</mapper>